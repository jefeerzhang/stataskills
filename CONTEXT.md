# Stata Skills

Agent 可调用的 Stata 技能仓库：每个 skill 先给最短命令链，再给可执行禁令。教材章节与方法签名是参考层，不是执行入口。

## Language

**强制路径**：
某种用户场景下，Agent 必须按顺序执行的最短命令链；匹配到第一条就停，禁止跳步或把工具箱全跑一遍。
_Avoid_: 工作流, 教程, 最小协议, 推荐步骤

**可执行禁令**：
黑名单条目，必须带替代命令；不是警告。
_Avoid_: 注意事项, 提醒, 最佳实践

**陷阱四件套**：
诊断层条目，固定写成「陷阱 → 触发 → Fix → 验证」。用来解释禁令为何成立，不替代强制路径。
_Avoid_: 陷阱速查（单独使用时）, 注意事项列表

**踢走**：
当前 skill 明确拒绝承接、并指向另一个 skill 或「本仓库尚未覆盖」的场景。分数线 / 年龄门槛 / 地理边界对 DID 即踢走。
_Avoid_: 不适用, 超出范围（不写替代去向时）
